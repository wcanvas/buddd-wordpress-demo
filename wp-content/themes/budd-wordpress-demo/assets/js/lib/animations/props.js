/**
 * Animation props
 */
export const PROPS = {
	wordByWordBottomToTop: {
		speed: 0.6,
		trigger: 0.5,
	},
	wordByWord: {
		speed: 0.3,
		trigger: 0.2,
	},
	lineByLine: {
		speed: 0.6,
		trigger: 0.2,
	},
	fadeInUpStagger: {
		speed: 0.1,
		trigger: 0.2,
	},
	revealFromDirection: {
		speed: 0.8,
		trigger: 0.4,
	},
};

/**
 * Show GSAP markers
 */
export const MARKERS = false;
